# BYD Metadata Dumper

Dumps `global-metadata.dat` for game, written in rust.

## Usage

```bash
Usage: byd-metadata-dumper [INPUT] [OUTPUT]

Arguments:
  [INPUT]   Input PE file path (default: GameAssembly.dll)
  [OUTPUT]  Output decrypted file path (default: global-metadata.dat)
```